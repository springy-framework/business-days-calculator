#!/bin/bash

docker-php-ext-configure calendar >> /dev/null 2>&1
docker-php-ext-install calendar >> /dev/null 2>&1
vendor/bin/phpcs --standard=PSR12 -n src
vendor/bin/phpstan analyse src --level 0
vendor/bin/phpunit --colors=always
